import 'package:flutter/material.dart';

class MeetingHome extends StatelessWidget {
  const MeetingHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Rendez-vous")));
  }
}